#include <stdio.h>  

int main(){

    //Declarando e inicializando o vetor de inteiros

    int numeros[5] = {1, 2, 3, 4, 5};

    //Declarando uma string (vetor de caracteres)

    char nome[10] = "Exemplo";

    //impressões:

    printf("Terceiro elemento do vetor: %d\n", numeros[2]);
    printf("Primeira letra do nome: %c\n", nome[0]);
    
    return 0;
}